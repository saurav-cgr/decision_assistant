import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Decision Memory Architecture",
  description: "Architecture diagram and interface mockups for Decision Memory.",
};

export default function Home() {
  return null;
}
