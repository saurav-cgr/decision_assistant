# Checker Verdicts

A criterion is `pass` only after the checker confirmed it against the PRIMARY
source (a run, a file, an endpoint), not the maker's self-assessment.

| ID | Iteration | Criterion | Method (primary source) | Verdict | Confidence | Date |
|----|-----------|-----------|-------------------------|---------|------------|------|
| D1 | 3 | pypdfium2 declared, pypdf absent | `grep -n pypdf api/pyproject.toml` shows only line 21 `pypdfium2==5.13.0`. The checker probe `.probe/d6_d8_probe.py` in the api image asserts `find_spec('pypdf') is None` and imports pypdfium2. | pass | high | 2026-09-24 |
| D2 | 4 | No pypdf or parser-selection references | Ran the contract grep independently. Remaining hits are the `PDF_PARSER_PROFILE` constant (the name plan.md and T008 prescribe) plus the `"pdf_parser"` profile key in `profiles.py`, and tests asserting that key or constant (two test names contain `pdf_parser`). No selection logic remains. `scripts/compare_pdf_parsers.py` is absent. Judged within the allowed exception. | pass | medium | 2026-09-24 |
| D3 | 3 | API suite passes, named tests exist | The checker ran `make test-api` itself: exit 0, 364 passed, 4 deselected (live_provider), 0 skipped. `--collect-only` lists all 8 named tests. Spot-read `test_non_pdf_sources_skip_worker_thread` and the scanned-empty integration test; both assert real behavior. | pass | high | 2026-09-24 |
| D4 | 3 | Web tests and build pass, new error code shown | The checker ran `make test-web` (exit 0, 36/36) and `npm run build` (exit 0). `IngestionStatus.tsx:4` maps `pdf_no_extractable_text`, with no `ocr_not_supported` entry. | pass | high | 2026-09-24 |
| D5 | 3 | Real Docling produces pdf_region only | Ran quickstart step 4 widened to all 4 PDF fixtures plus the Atlas PDF. Block counts: atlas 9, digital 9, multi-column 2, scanned 1 (OCR text present; the Tesseract OSD warning is benign), table-heavy 3. Every file yields exactly `{'pdf_region'}`. | pass | high | 2026-09-24 |
| D6 | 3 | PDF error contract holds on real files, messages sanitized | The checker-authored probe `.probe/d6_d8_probe.py` builds a reportlab user-password PDF, corrupt bytes, a truncated PDF, a blank page, and a 0-byte file, then calls `parse_document`. Codes: pdf_password_protected, pdf_parse_failed, pdf_parse_failed, pdf_no_extractable_text, pdf_parse_failed. All have `retryable=False`, and no message contains docling, pdfium, or a path. | pass | high | 2026-09-24 |
| D7 | 3 | Non-PDF behavior unchanged | `git diff parsers.py` has only the pypdf import, `_parse_pdf_document`, and `_reconstruct_pdf_lines` hunks; md, txt, and docx functions are untouched. Test diffs touch only PDF tests. `.probe/d7_probe.py` forces `model_timeout_seconds=1e-9`: every Atlas md, docx, and txt file parses, and only the PDF raises `pdf_parse_timeout`. | pass | high | 2026-09-24 |
| D8 | 3 | Corpus profile and settings are Docling-only | The probe checks that the profile equals the exact docling dict and `CURRENT_CHUNKING_PROFILE["pdf_parser"]`. `Settings()` has no `pdf_parser`, even with `PDF_PARSER=pypdf` set in the environment. `resolve_corpus_profile` called with 3 positional arguments raises TypeError. | pass | high | 2026-09-24 |
| D9 | 3 | Frozen contracts and file size hold | `test_frozen_parser_and_chunker_contract` is collected and passes. `wc -l` on all touched files gives max 478 (`service.py`), with parsers.py at 426. | pass | high | 2026-09-24 |
| D10 | 4 | README states the Docling behavior | Read the diff for Key Features, Technology Stack, Installation (701 MB / 2.6 GB, bundled, no runtime download), Privacy & Limitations, and Trade-offs. Each required statement is present. A grep finds no "embedded text", no scanned-PDF rejection, and no claim that tables or multi-column text are out of scope. | pass | high | 2026-09-24 |
| D11 | 4 | AGENTS.md and comparison record updated | AGENTS.md has the plain heading, no "Default remains", and a "PDF quality gate" section with ≥ 0.90 and ≤ 0.05. `parser_comparison.md` keeps its table and JSON (the diff only touches Reproduction), adds "Decision (2026-09-24)" with 0.85/0.90 and 0.10/0.05, and has no `--parser` command. | pass | high | 2026-09-24 |
| D12 | 6 | Reset, reingest, and SC-003 benchmark hold | The checker queried Postgres directly. Run `d319e454-1d8f-48cb-9ce2-4785eeebf70c` exists: completed 20/20, passage_hybrid, atlas-v3, rerank off, 6-document corpus_snapshot. Its aggregate_metrics match the record: top_five 1.0, citation 0.9643, abstention 0.80. Conflict rate recomputed from result states is 1/20 = 0.05. All 6 active versions carry the docling pdf_parser profile, and all 28 passages have an `embedding_cache_id`. The only `supports_claim:false` check is atlas-001 on `01-product-plan.md` (Markdown, the date "May"), which confirms it is not a PDF defect. The discarded first run `2e4b6f6b` is present with 1 question failure, as disclosed. The literal SC-003 bar (citation ≥ 1.00) is NOT met. The pass rests on a user waiver made in the maker session, which the checker cannot verify from a primary source. | uncertain | medium | 2026-09-24 |
| D13 | 6 | No secrets or unrelated files staged | The checker ran `git diff --cached --name-only` (empty, nothing staged). `git check-ignore` shows `.env` ignored by `.gitignore:1`. `.env.gemini.bak` is untracked and unstaged. The design doc is untracked, with mtime 2026-08-19 (untouched). The `.gitignore` change was committed in 07bcc96 at 13:43, before the loop's spec.md existed (13:51), so it was not changed by the loop. Grepping the diff for keys finds only the reportlab test password "secret" in a fixture. `.env.example` only drops `PDF_PARSER`. | pass | high | 2026-09-24 |
