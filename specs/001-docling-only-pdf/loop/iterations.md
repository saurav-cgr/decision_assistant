# Maker Iterations

Append-only. One record per /speckit.loop.run iteration. The maker never marks
the loop done — it records what it attempted and which criteria it believes are
now ready for the checker.

<!-- Record format:
## Iteration <n> — <date>
- Targeted criteria: <D-ids>
- Change: <what was produced/modified, with file pointers>
- Maker self-assessment: <which criteria the maker believes are now ready>
- Open questions / risks: <anything the checker or a human should look at>
- Handoff: ready-for-check
-->

## Iteration 1 — 2026-09-24
- Targeted criteria: Phase 1 Setup (T001, T002); no D-id yet (prerequisite for D1)
- Worktree: in place
- Change: `api/pyproject.toml:22` added `"pypdfium2==5.13.0",` between `pypdf==5.9.0` and `python-docx==1.2.0`. Rebuilt `docker compose build api`. Verified `importlib.metadata.version('pypdfium2') == "5.13.0"` inside the built image. pypdf left in place (removal is T018).
- Maker self-assessment: T001 and T002 complete. D1 (`api/pyproject.toml` declares pypdfium2 and does not declare pypdf) is NOT yet ready — pypdf removal is deliberately deferred to T018 per memory M-004. No D-id should move to maker-ready this iteration.
- Open questions / risks: none. Build succeeded cleanly; Docling model download ran during build as expected (cached artifacts, no runtime fetch).
- Handoff: ready-for-check (informational only; no criterion targeted for grading yet — next iteration proceeds to Phase 3 US1 tests, T003-T007)

## Iteration 2 — 2026-09-24
- Targeted criteria: Phase 3 US1 tests (T003-T007); prerequisite for D1-D9
- Worktree: in place
- Change:
  - `api/tests/unit/test_chunking_profiles.py`: swapped `PDF_PARSER_PROFILES` import for `PDF_PARSER_PROFILE`; rewrote `test_corpus_profile_includes_pdf_parser_contract` to a single-profile assertion plus `PDF_PARSER_PROFILE["name"] == "docling"`; `test_settings_default_is_baseline` now asserts `not hasattr(settings, "pdf_parser")` and `CURRENT_CHUNKING_PROFILE["pdf_parser"]["name"] == "docling"`; deleted `test_settings_accepts_valid_pdf_parser` and `test_settings_rejects_invalid_pdf_parser`.
  - `api/tests/unit/test_pdf_parser.py`: dropped `pypdf` import and `_reconstruct_pdf_lines` import; deleted `test_pdf_line_reconstruction_joins_wrapped_sentences`, `test_pdf_parser_preserves_page_numbers_and_offsets`, `test_explicit_pypdf_selection_preserves_legacy_pdf_pages`, `test_pdf_pages_are_page_blocks_with_hard_boundaries`; simplified `test_pdf_parser_dispatches_to_docling` (no settings monkeypatch); dropped `pdf_parser="docling"` from the worker-thread and timeout `SimpleNamespace` settings stubs; added `test_non_pdf_sources_skip_worker_thread`; replaced `test_pdf_without_embedded_text_requires_ocr` with `test_pdf_without_extractable_text_is_reported` (stubs `docling.document_converter.DocumentConverter` to return a zero-item SUCCESS result); rewrote `test_encrypted_pdf_returns_password_error` to build the encrypted fixture with `reportlab` instead of `pypdf`, and to assert Docling conversion is never reached; added sanitized-message assertions (`"docling-parse"`/`"PDFium"` not in message) to the encrypted/corrupt/no-extractable-text tests; removed the now-unused `SCANNED_EMPTY_PDF` constant.
  - `api/tests/integration/test_docling_pdf_ingestion.py`: removed the `config.get_settings` monkeypatch and now-unused `SimpleNamespace`/`config` imports; dropped the `ocr_not_supported` guard (bare propagation); added `test_docling_reports_scanned_empty_pdf_without_text` against `tests/fixtures/scanned-empty.pdf`.
- Maker self-assessment: T003-T006 complete. T007 run and confirmed: `test_chunking_profiles.py` fails at collection (`ImportError: cannot import name 'PDF_PARSER_PROFILE'`) — expected, profiles.py not yet changed (T008). `test_pdf_parser.py`/`test_docling_pdf_ingestion.py` (run together, since the collection error blocks a combined run): 6 failed, 13 passed — all 6 failures trace to the still-live `pdf_parser`-gated dispatch in `parsers.py`/`service.py` and the still-pypdf-only error mapping (not yet unconditional Docling dispatch, no `_preflight_pdf`, no `pdf_no_extractable_text` code). This is the expected red state before T008-T015. No D-id is maker-ready yet — implementation is next.
- Open questions / risks: the real integration run downloads/loads Docling weights and runs real Tesseract OCR (slow, ~90s for this combined run); `test_docling_reports_scanned_empty_pdf_without_text` currently gets `pdf_parse_failed` where T014 will produce `pdf_no_extractable_text` — confirms T014 is the right fix. No secrets touched; only test files edited.
- Handoff: ready-for-check (informational only; still no criterion at maker-ready — next iteration is Phase 3 implementation, T008-T018)

## Iteration 3 — 2026-09-24
- Targeted criteria: D1, D2, D3, D4, D5, D6, D7, D8, D9 (Phase 3 US1 implementation, T008-T018)
- Worktree: in place
- Change:
  - `api/src/decision_assistant/ingestion/profiles.py`: replaced `PDF_PARSER_PROFILES` dict with single `PDF_PARSER_PROFILE` constant (docling, 2.130.0, tesseract-eng, docling-layout-v1); `resolve_corpus_profile` dropped its `pdf_parser` parameter and the "Unknown PDF parser" check, now always embeds `PDF_PARSER_PROFILE.copy()`.
  - `api/src/decision_assistant/config.py`: removed the `PDF_PARSER_PROFILES` import, the `pdf_parser: str = "pypdf"` field, and `_validate_pdf_parser`. `extra="ignore"` unchanged (absorbs a stale `PDF_PARSER` env var).
  - Removed the `settings.pdf_parser` argument from `resolve_corpus_profile(...)` call sites in `main.py:220`, `retrieval/router.py:59`, `answering/router.py:65`, `evaluation/router.py:46`, `documents/router.py:71`.
  - `.env.example`: removed `PDF_PARSER=pypdf`.
  - `api/src/decision_assistant/ingestion/parsers.py`: removed `from pypdf import PdfReader` / `from pypdf.errors import PdfReadError`; deleted `_parse_pypdf_document` and `_reconstruct_pdf_lines`; `_parse_pdf_document` now calls `_parse_docling_pdf_document` unconditionally with no `get_settings` import. Kept `_normalize_extracted_text`, `_heading_key`, `_assemble_document`, the `_docling_*` shims, and all frozen signatures.
  - `api/src/decision_assistant/ingestion/docling_parser.py`: added `_preflight_pdf(path)` (lazy `pypdfium2` import; `PdfDocument(...).close()`; `"password"` in the lowercased `PdfiumError` text → `pdf_password_protected`, else `PdfiumError`/`OSError` → `pdf_parse_failed`), called first in `parse_docling_pdf` before building `PdfPipelineOptions`, outside the broad `except Exception` block. Changed the zero-block branch to raise `pdf_no_extractable_text` / "PDF contains no extractable text".
  - `api/src/decision_assistant/ingestion/service.py:69-81`: `_parse_for_ingestion` now branches on `source_path.suffix.lower() == ".pdf"` instead of `get_settings().pdf_parser == "docling"`; removed the `pdf_parser` settings check.
  - Deleted `scripts/compare_pdf_parsers.py` (T016); confirmed only historical docs (`evaluation/parser_comparison.md`, spec/tasks/plan.md) still reference it.
  - `web/src/components/IngestionStatus.tsx`: replaced the `ocr_not_supported` entry with `pdf_no_extractable_text: "This PDF contains no readable text."`.
  - `web/src/pages/Workspace.test.tsx`: updated the OCR fixture's error code to `pdf_no_extractable_text` (renamed doc id `ocr-pdf` → `empty-pdf`) and the message assertion to `/contains no readable text/i`.
  - `api/pyproject.toml`: removed `"pypdf==5.9.0",` after rebuilding and confirming green; rebuilt `docker compose build api` a second time.
  - Collateral fixes (outside tasks.md's explicit list, needed to keep `make test-api` green now that every PDF parse is Docling-only): `api/tests/unit/test_chunking.py` — renamed `test_chunking_preserves_pdf_page_locator_kind` to `test_chunking_preserves_pdf_region_locator_kind` and updated its assertion, since fresh parsing of `tests/fixtures/text.pdf` now always yields `pdf_region` (only stored/gold `pdf_page` locators are still read-side supported, per M-006). `api/tests/unit/test_evaluation_fixture.py` — added a local `_locator_matches` helper (page-level gold `pdf_page` locator matches either `pdf_page` or `pdf_region` passage locator by page, mirroring `evaluation/service.py::_locator_covers`) and used it in `test_every_gold_quote_and_locator_resolves_in_real_sample_files` instead of raw dict equality, since `04-q3-planning.pdf`'s gold locator (`atlas-009`, kind `pdf_page`) no longer resolves by strict equality against Docling's `pdf_region` output.
- Maker self-assessment:
  - D1 ready: `grep -n "pypdf" api/pyproject.toml` shows only `pypdfium2==5.13.0`; rebuilt image confirms `importlib.util.find_spec('pypdf') is None` and `import pypdfium2` succeeds.
  - D2 ready (with the documented exception): `grep -rniI "pypdf\b\|pdf_parser\b\|PDF_PARSER\|PDF_PARSER_PROFILES\|ocr_not_supported\|_reconstruct_pdf_lines\|compare_pdf_parsers" api/src api/pyproject.toml api/tests scripts web/src .env.example README.md AGENTS.md` — remaining hits are only the `"pdf_parser"` key in `profiles.py`'s corpus-profile dict and the tests that assert it (checker should verify this directly); `scripts/compare_pdf_parsers.py` no longer exists. README/AGENTS.md not yet updated (US2, next iteration) so those may still show old wording — checker should re-grep after US2 lands.
  - D3 ready: `make test-api` exits 0, 364 passed, includes all eight named tests (`test_encrypted_pdf_returns_password_error`, `test_corrupt_pdf_returns_parser_specific_error`, `test_pdf_without_extractable_text_is_reported`, `test_non_pdf_sources_skip_worker_thread`, `test_pdf_parser_dispatches_to_docling`, `test_docling_parse_timeout_is_sanitized`, `test_corpus_profile_includes_pdf_parser_contract`, `test_docling_reports_scanned_empty_pdf_without_text`).
  - D4 ready: `make test-web` exits 0 (36 tests, 10 files) and `docker compose run --rm web npm run build` exits 0; `IngestionStatus.tsx` maps `pdf_no_extractable_text` and has no `ocr_not_supported` entry (verified directly).
  - D5 ready: quickstart step 4 reports `04-q3-planning.pdf` (9 blocks) and `scanned-english.pdf` (1 block), both `{'pdf_region'}`.
  - D6 believed ready but **not independently probed** — the checker must write its own probe per the contract (maker ran the equivalent via unit tests only, not a from-scratch reportlab/corrupt/blank probe).
  - D7 ready: `git diff` on `parsers.py` shows only the PDF path removed/changed; `_parse_text_document`, `_parse_docx_document`, and their existing tests are untouched. `test_non_pdf_sources_skip_worker_thread` passes.
  - D8 ready: `resolve_corpus_profile("baseline","passage_hybrid")["pdf_parser"]` verified via test to equal `PDF_PARSER_PROFILE` (docling/2.130.0/tesseract-eng/docling-layout-v1) and matches `CURRENT_CHUNKING_PROFILE["pdf_parser"]`; `Settings()` has no `pdf_parser` attribute (asserted by `test_settings_default_is_baseline`); calling `resolve_corpus_profile` with 3 positional args now raises `TypeError` (only 2 accepted) — not separately re-verified live by the maker this iteration, should hold given the signature change.
  - D9 ready: `test_frozen_parser_and_chunker_contract` passes (in the 364); `wc -l` on every ingestion file is below 500 (max is service.py at 478).
- Open questions / risks:
  - The two collateral test fixes (`test_chunking.py`, `test_evaluation_fixture.py`) are outside tasks.md's literal scope but were required to keep the full suite green under Docling-only parsing — checker should confirm these are legitimate consequences of D5/D6 (new ingestion is pdf_region-only) and not scope creep.
  - D2's "no matches outside the allowed exception" claim needs re-verification after US2 (README/AGENTS.md, next iteration) since those files still have old pypdf/parser wording.
  - D6's checker-authored probe (encrypted/corrupt/blank via a fresh script, not the maker's tests) has not been run by the maker — by design, per the contract.
  - D12/D13 (reset+reingest benchmark, secrets) are untouched — D12 is human-gated and D13 is a final-commit check, both out of scope for this iteration.
- Handoff: ready-for-check. D1, D3, D5, D7, D8, D9 are maker-ready with direct verification; D2, D4, D6 are maker-ready but flagged above for extra checker scrutiny (D2 pending US2, D4 not directly re-verified end-to-end, D6 needs the checker's own probe per contract).

## Iteration 4 — 2026-09-24
- Targeted criteria: D2, D10, D11 (Phase 4 US2, T019-T022)
- Worktree: in place
- Change:
  - `README.md`: Key Features "Document ingestion" row rewritten (Docling layout-aware parsing, tables, multi-column, local OCR, page/region locators, encrypted/corrupt/blank PDF errors). Technology Stack gained a "PDF parsing" row (Docling 2.130.0 + Tesseract English OCR; pypdfium2 for preflight). Installation "Build and start" gained a note that `/opt/docling-models` is bundled at build time, no runtime download, ~701 MB models / ~2.6 GB image. Privacy & Limitations "Supported formats" paragraph rewritten: Markdown/text/DOCX/PDF (digital or scanned English), local PDF parsing and OCR, encrypted/password-protected PDFs and macros/link-following/password-recovery out of scope, region locators stored but not highlighted. Trade-offs' "not pixel-perfect PDF coordinates" line replaced with the Docling image-size/CPU tradeoff and the not-yet-highlighted region-coordinate note.
  - `evaluation/parser_comparison.md`: kept the header, table, and both raw JSON records unchanged as history. Replaced "Reproduction" with a note that pypdf and `scripts/compare_pdf_parsers.py` were removed 2026-09-24 and the historical pypdf run is reproducible only from git history. Appended a "Decision (2026-09-24)" section stating Docling is the only parser, the parity gate was waived (citation correctness 1.00 vs 0.9655; abstention 0.85 vs 0.90; conflict 0.10 vs 0.05), and the open SC-006 follow-up gate (abstention ≥ 0.90, conflict ≤ 0.05 after full reingestion).
  - `AGENTS.md`: renamed "## Docling PDF parsing (pdf-improvement branch)" to "## Docling PDF parsing"; rewrote the "Parser contract is reset-required" bullet to drop the pypdf/docling switch language and "Default remains `pypdf`"; rewrote "Locator kinds" to say new ingestion emits only `pdf_region`, with `pdf_page` staying a supported read/gold-locator kind matching by page; replaced "## Parser comparison evaluation" with "## PDF quality gate" naming `evaluation/parser_comparison.md` as the audit record and the SC-006 follow-up targets. Left the "Loop sensor gates" wording unchanged (it doesn't mention pypdf).
- Maker self-assessment:
  - D10 ready: README Key Features, Technology Stack, Installation, Privacy & Limitations, and Trade-offs sections all state the required Docling behavior (layout/table/multi-column parsing, local OCR, no runtime download, approximate model/image size, encrypted PDFs unsupported, region locators stored-not-highlighted) and contain none of the forbidden claims (scanned-PDFs-rejected, embedded-text-only, tables/multi-column out of scope) — confirmed by direct grep and read-through, no leftover matches.
  - D11 ready: AGENTS.md heading no longer says "(pdf-improvement branch)"; no "Default remains `pypdf`" text; "PDF quality gate" section present with abstention ≥0.90/conflict ≤0.05 targets. `evaluation/parser_comparison.md` keeps the original table/JSON, has the "Decision (2026-09-24)" section with the exact gap values, and no runnable `--parser pypdf` command remains (the compare script invocation was replaced with a removal note).
  - D2 ready (with the documented exception): re-ran the exact checker grep (`pypdf\b|pdf_parser\b|PDF_PARSER|PDF_PARSER_PROFILES|ocr_not_supported|_reconstruct_pdf_lines|compare_pdf_parsers` across `api/src api/pyproject.toml api/tests scripts web/src .env.example README.md AGENTS.md`); every remaining hit is the `PDF_PARSER_PROFILE` constant / `"pdf_parser"` dict key in `profiles.py`, or test names/assertions referencing that same key (`test_pdf_parser_dispatches_to_docling`, `test_corpus_profile_includes_pdf_parser_contract`, `test_chunking_profiles.py`'s import and assertions). `scripts/compare_pdf_parsers.py` does not exist. Removed a stray literal "pypdf" from the new AGENTS.md PDF-quality-gate bullet during self-check to keep the sweep clean.
- Open questions / risks: D2's allowed exception in loop.md text says "the `pdf_parser` key... and in tests that assert it" — the maker's remaining hits also include two test *function names* containing `pdf_parser` (not just key assertions); flagging this for the checker to judge whether that still counts as the same allowed exception (the maker believes it does, since both functions directly test the `pdf_parser` profile key/field).
- Handoff: ready-for-check. D2, D10, D11 now maker-ready, joining D1, D3-D9 from iteration 3. Full status: D1 maker-ready, D2 maker-ready (flagged), D3 maker-ready, D4 maker-ready, D5 maker-ready, D6 maker-ready (needs checker's own probe), D7 maker-ready, D8 maker-ready, D9 maker-ready, D10 maker-ready, D11 maker-ready, D12 pending (human-gated), D13 pending (final pre-commit check). Run `/speckit.loop.check` next, as a separate session/agent. 11 iterations left of 15.

## Iteration 5 — 2026-09-24
- Targeted criteria: D12
- Worktree: in place
- Change: user explicitly approved the D12 PostgreSQL reset. Ran the corpus reset (`docker compose stop api web`; `dropdb`/`createdb` in the `db` container; `alembic upgrade head`, clean 11-revision chain to `0011_conversations`; `docker compose up -d api web --wait`, both healthy). `scripts/compare_retrieval_strategies.py` could not be used as-is: its `_require_matching_corpus` check reads `chunking_profile` from the API's `ActiveVersionDetail` response, which does not include that field (a pre-existing gap in `documents/schemas.py`, unrelated to this loop's touched files) — the field is always `null` over that path, so the script's own equality check always fails regardless of the actual profile. Verified the real profile directly in Postgres instead (`select chunking_profile from document_versions where state='active'`): all 6 active versions carry the exact Docling `CURRENT_CHUNKING_PROFILE`, including `pdf_parser: {"name":"docling","version":"2.130.0","ocr":"tesseract-eng","layout":"docling-layout-v1"}`. Reingested Atlas (6 documents, 28 passages) and ran the evaluation directly against the API (`POST /evaluations/runs`, `strategy=passage_hybrid`, `dataset_version=atlas-v3`, reranking disabled) instead of through the broken script wrapper. First run hit a transient `Model provider unavailable` failure on one question (atlas-010) — discarded, not counted. Clean rerun: `run_id d319e454-1d8f-48cb-9ce2-4785eeebf70c`, top_five_hit_rate 1.00, citation_correctness 0.9643, abstention_accuracy 0.80, conflict_rate 0.05 (computed from `generated_output.state` counts; the API's `aggregate_metrics` does not expose conflict rate directly). Recorded the run and a metrics table in `evaluation/parser_comparison.md` under "## D12 reset-and-reingest benchmark (2026-09-24)".
- Maker self-assessment: D12 is NOT ready. SC-003's blocking bar (citation_correctness ≥ 1.00) was not met (0.9643). The single miss is on `atlas-001`, a Markdown source, not a PDF — the judge docked a date-attribution nuance unrelated to Docling, and the historical pypdf baseline also missed this same bar (0.9655), so this is not a Docling regression. top_five_hit_rate did meet its 1.00 bar. SC-006 (non-blocking): conflict_rate 0.05 meets target, abstention_accuracy 0.80 misses the 0.90 target and stays open as already documented. Did not retry past the one clean rerun to avoid resampling toward a lucky pass against a tight judge-model threshold — reported the real number instead.
- Open questions / risks: (1) D12's literal SC-003 bar is unmet; needs a human decision on whether to treat this the same way the SC-006 gap was already waived by the project owner, or require further investigation/retries. (2) `scripts/compare_retrieval_strategies.py` / `ActiveVersionDetail` has a real bug (chunking_profile never serialized) independent of this loop; not fixed here since it is outside tasks.md scope and the loop's allowed-tools list does not cover API schema changes — flagging for a separate fix, not attempted.
- Handoff: escalated-to-human. D12 reset executed with explicit approval; benchmark ran and is recorded, but the blocking SC-003 citation-correctness bar was not met (0.9643 vs ≥1.00). D12 left `pending`, not `maker-ready` — reporting the real result rather than asserting readiness. D13 still not attempted. 10 iterations left of 15.

## Iteration 6 — 2026-09-24
- Targeted criteria: D12, D13
- Worktree: in place
- Change: D13 — ran `git status --porcelain` and `git diff --cached --name-only`. Nothing staged. `.env.gemini.bak` remains untracked and unstaged. `docs/superpowers/specs/2026-08-19-ingestion-chunking-correctness-design.md` remains untracked, untouched. No secret/key/token files staged. D12 — user reviewed iteration 5's benchmark result and explicitly waived the SC-003 citation-correctness gap (0.9643 vs ≥1.00), same basis as the pre-existing SC-006 waiver: the single miss is on a non-PDF source and is judge-grading noise, not a Docling defect, and the pypdf baseline missed the same bar. Recorded the waiver in `evaluation/parser_comparison.md` under the D12 section.
- Maker self-assessment: D12 ready (waived by user, top_five_hit_rate 1.00 met, citation_correctness gap explicitly waived, run_id d319e454-1d8f-48cb-9ce2-4785eeebf70c recorded). D13 ready (git status/staged-files check clean, no secrets, no untouched-file violations).
- Open questions / risks: none new. The checker should independently re-verify the waiver was actually requested by the user (it was, in this session) rather than asserted unilaterally by the maker, and should re-run `git status`/`git diff --cached` itself for D13.
- Handoff: ready-for-check. D12 and D13 now maker-ready, joining D1-D11 (already checker-pass per loop.md). All 13 done-criteria have maker-side work complete. Run `/speckit.loop.check` next, as a separate session/agent, for D12 and D13. Then `/speckit-loop-guard` for human sign-off to close the loop. 9 iterations left of 15.
